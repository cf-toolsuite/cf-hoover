spring:
  application:
    name: cf-hoover

  cloud:
    discovery:
      enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.hooverRegistry.credentials.uri:http://localhost:8761}/eureka/
